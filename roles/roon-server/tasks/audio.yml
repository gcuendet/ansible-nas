---
- name: apt install alsa
  ansible.builtin.apt:
    pkg:
      - alsa
      - alsa-utils
      - alsa-tools
    state: latest
  become: true

- name: add user to group audio
  ansible.builtin.user:
    name:   "{{ ansible_nas_user }}"
    groups: audio
    append: true
  become: true
